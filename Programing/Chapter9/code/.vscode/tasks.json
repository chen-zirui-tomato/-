{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "C/C++: g++ 生成活动文件",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",  // 生成调试信息
                "${file}",
                "-o",
                "${workspaceFolder}/a.out"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}